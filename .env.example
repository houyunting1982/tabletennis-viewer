# Local dev uses /catalog.json by default.
# For production / CDN catalog, set:
# VITE_CATALOG_URL=https://cdn.your-domain.com/catalog.json

# Optional: override local Biba JPG folder (default: assets/Biba JPG)
# BIBA_ASSETS_DIR=/path/to/Biba JPG
